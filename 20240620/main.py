import requestsfrom datetime import datetimeUSERNAME = 'test'TOKEN = 'testtesttesttesttest'GRAPH_ID = 'graph1'pixela_endpoint = "https://pixe.la/v1/users"user_parameters = {    "token": TOKEN,    "username": USERNAME,    "agreeTermsOfService": "yes",    "notMinor": "yes"}# response = requests.post(url=pixela_endpoint, json=user_parameters)# print(response.text)graph_endpoint = f'{pixela_endpoint}/{USERNAME}/graphs'graph_config = {    "id": "graph1",    "name": "Python Study Graph",    "unit": "hour",    "type": "float",    "color": "sora"}headers = {    "X-USER-TOKEN": TOKEN}# response = requests.post(url=graph_endpoint, json=graph_config, headers=headers)# print(response.text)pixel_creation_endpoint = f'{pixela_endpoint}/{USERNAME}/graphs/{GRAPH_ID}'pixel_creation_params = {    "date": datetime.now().strftime('%Y%m%d'),    "quantity": "4.5",}# response = requests.post(url=pixel_creation_endpoint, json=pixel_creation_params, headers=headers)# print(response.text)pixel_update_endpoint = f'{pixela_endpoint}/{USERNAME}/graphs/{GRAPH_ID}/{datetime.now().strftime("%Y%m%d")}'pixel_update_params = {    "quantity": "1.8"}# response = requests.put(url=pixel_update_endpoint, json=pixel_update_params, headers=headers)# print(response.text)pixel_delete_endpoint = f'{pixela_endpoint}/{USERNAME}/graphs/{GRAPH_ID}/{datetime.now().strftime("%Y%m%d")}'response = requests.delete(url=pixel_delete_endpoint, headers=headers)print(response.text)